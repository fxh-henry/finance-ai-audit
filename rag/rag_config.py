# -*- coding: utf-8 -*-
# ============================================================================
# RAG模块配置
# ============================================================================
# 这里只放RAG模块内部的参数，项目级配置（公司信息、API密钥等）在 config/settings.py
# ============================================================================

# 检索结果数量：每种检索（BM25/向量）返回前N个高分文本块
TOP_K = 3

# 文本分块参数
CHUNK_MAX_LEN = 300   # 单个文本块最大字符长度
OVERLAP_SENT = 2      # 块与块之间重叠保留句子数量

# 混合检索权重：BM25分数权重alpha，向量分数权重(1-alpha)
BM25_WEIGHT_ALPHA = 0.6

# BM25专有名词加分权重
TERM_WEIGHT_SCALE = 2.0

# Hugging Face 镜像地址（国内用户使用，加速模型下载）
HF_ENDPOINT = "https://hf-mirror.com"

# 向量模型名称（m3e-small是轻量级中文embedding模型）
MODEL_NAME = 'moka-ai/m3e-small'

# 是否启用缓存（分块缓存、BM25缓存、向量缓存）
USE_CACHE = True
